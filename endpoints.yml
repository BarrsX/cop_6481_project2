# endpoints.yml
action_endpoint:
  url: "http://localhost:5055/webhook"

tracker_store:
  type: SQL
  dialect: "postgresql"
  url: "localhost"
  db: "rasa"
  username: "username"
  password: "password"
