from rasa_sdk import Action, Tracker
from rasa_sdk.executor import CollectingDispatcher
from typing import Any, Text, Dict, List

class ActionCheckBalance(Action):
  def name(self) -> Text:
    return "action_check_balance"

  def run(self, dispatcher: CollectingDispatcher,
      tracker: Tracker,
      domain: Dict[Text, Any]) -> List[Dict[Text, Any]]:
    # Call the API to check the balance
    # For the sake of this example, let's assume the balance is $1000
    balance = "$1000"
    dispatcher.utter_message(text=f"Your balance is {balance}")

class ActionTransferFunds(Action):
  def name(self) -> Text:
    return "action_transfer_funds"

  def run(self, dispatcher: CollectingDispatcher,
      tracker: Tracker,
      domain: Dict[Text, Any]) -> List[Dict[Text, Any]]:
    # Call the API to transfer funds
    # For the sake of this example, let's assume the transfer was successful
    dispatcher.utter_message(text="Funds transferred successfully")